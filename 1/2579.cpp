#include <iostream>
using namespace std;
// D[i]
// D[i] = max(D[i+1] D[i+2])
// 3개 연속은 안된다.
// 마지막 계단은 반드시
// N = D[n] + i
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    return 0;
}